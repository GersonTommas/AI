<Window x:Class="AI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AI"
        mc:Ignorable="d"
        Title="MainWindow" WindowStyle="None" AllowsTransparency="True" Background="Transparent" SizeToContent="WidthAndHeight" MouseLeftButtonDown="Window_Drag">
    <Border BorderBrush="DarkGray" Background="#209080" CornerRadius="25" Padding="10">
        <Grid Width="400">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border CornerRadius="15" BorderThickness="1" Background="DarkGray">
                <TextBox Background="DarkGray" Text="Listening..." />
            </Border>

            <Button Content="???" Grid.Column="1" />
        </Grid>
    </Border>
</Window>
